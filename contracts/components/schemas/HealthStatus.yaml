type: object
required: [status, service, timestamp]
properties:
  status:
    type: string
    enum: [healthy, degraded, unavailable]
  service:
    type: string
  timestamp:
    type: string
    format: date-time
