type: object
required: [code, message]
properties:
  code:
    type: string
    examples: [bad_request, unauthorized, not_found, internal_error]
  message:
    type: string
  details:
    type: object
    additionalProperties: true
